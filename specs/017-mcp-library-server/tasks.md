# Tasks: Read-Only MCP Library Server

## Dependencies

- Constitution 1.1.0 permits a read-only local stdio MCP server.
- Owner approved official Python MCP SDK installation.
- PostgreSQL contains indexed papers.

## Tasks

- [x] T001 Activate `017` and record MCP SDK exact pins.
- [x] T002 Add bounded indexed-only list/get helpers to `app/db/papers.py`.
- [x] T003 Add `app/mcp_server.py` with exactly three read-only stdio tools.
- [x] T004 Document local launch/configuration and public HTTP deferral.
- [x] T005 Build the app image from exact pins.
- [x] T006 Verify initialize, tool discovery, and all three calls through a real
      MCP stdio client.
- [x] T007 Record completion in `specs/README.md` and `RAG_UPGRADE_PLAN.md`.
