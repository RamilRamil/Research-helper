# Implementation Plan

- `answer.py`: context label `[arxiv_id]`; prompt bans numeric cites.
- `papers.py`: `arxiv_ids_sharing_tags` then if empty `categories &&`
  (function removed; graph hop is `011` communities).
- Router/graph unchanged besides neighbor SQL.

No requirements.txt change.
