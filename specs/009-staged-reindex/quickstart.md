# Quickstart

1. Apply `scripts/chunk_gen.sql`. Rebuild `app`.
2. `/reindex 2607.20124` while paper is `indexed`.
3. Confirm bot says rebuilt / indexed ok. `/ask 2607.20124` still answers.
4. Incomplete `/reindex` on a `failed` id still works as 001.
