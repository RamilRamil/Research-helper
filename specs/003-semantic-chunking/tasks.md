# Tasks: Semantic Adaptive Chunking

- [x] T001 Replace character window in `app/rag/chunker.py` with paragraph/sentence pack + overlap.
- [x] T002 Dry-run `chunk_text` on one existing PDF via app image; record mid-word rate in `implementation-notes.md`.
- [x] T003 Point `.specify/feature.json` and `specs/README.md` at `003-semantic-chunking`.
